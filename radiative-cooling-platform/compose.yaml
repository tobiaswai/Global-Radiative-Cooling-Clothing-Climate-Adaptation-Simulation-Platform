services:
  postgres:
    image: postgres:17-alpine
    container_name: rc-postgres
    restart: unless-stopped
    environment:
      POSTGRES_DB: radiative_cooling
      POSTGRES_USER: rc_user
      POSTGRES_PASSWORD: rc_password
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test:
        - CMD-SHELL
        - pg_isready -U rc_user -d radiative_cooling
      interval: 5s
      timeout: 5s
      retries: 10

  redis:
    image: redis:7-alpine
    container_name: rc-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    healthcheck:
      test:
        - CMD
        - redis-cli
        - ping
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres_data:
  redis_data: